/** @type {import('./sanity-types').SanityPublicConfig} */
window.SANITY_CONFIG = {
  projectId: "abnka2pd",
  dataset: "production",
  apiVersion: "2024-06-01",
  useCdn: true,
  skipProxy: true,
  // Optional: set via <meta name="sanity-token" content="..."> for draft/private datasets
};
